# Rename this file to secrets.toml and fill in your values.
# NEVER commit secrets.toml — it is in .gitignore.
# On Streamlit Cloud, add these under Settings → Secrets.

# Admin PIN (required — used to access admin/network/schema tabs)
ADMIN_PIN = "change_me_please"

# Google AdSense Auto Ads — publisher ID is already set in sporting.py
# No slot needed — Google Auto Ads handles placement automatically.
# ADSENSE_CLIENT = "ca-pub-8003312242019311"  (already hardcoded in sporting.py)
